/// Denominaciones por defecto (fallback CUP, igual que la web).
class BillDenominations {
  BillDenominations._();

  static const List<double> cup = [
    1000, 500, 200, 100, 50, 20, 10, 5, 3, 1,
  ];
}
